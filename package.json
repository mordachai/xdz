{
  "name": "xdz",
  "version": "0.1.0",
  "description": "CSS compiler for the Xeno Dead Zone system",
  "scripts": {
    "build": "sass src/scss/xdz.scss css/xdz.css --style=expanded --no-source-map",
    "watch": "sass src/scss/xdz.scss css/xdz.css --style=expanded --source-map --watch",
    "pack": "npm run pack:npcs && npm run pack:vehicles && npm run pack:assets && npm run pack:weapons && npm run pack:extragear && npm run pack:tables && npm run pack:macros && npm run pack:journals",
    "unpack": "npm run unpack:npcs && npm run unpack:vehicles && npm run unpack:assets && npm run unpack:weapons && npm run unpack:extragear && npm run unpack:tables && npm run unpack:macros && npm run unpack:journals",
    "pack:npcs": "fvtt package pack npcs --in packs/_source/npcs --out packs --type System --id xdz -v",
    "unpack:npcs": "fvtt package unpack npcs --in packs --out packs/_source/npcs --type System --id xdz -v --clean",
    "pack:vehicles": "fvtt package pack vehicles --in packs/_source/vehicles --out packs --type System --id xdz -v",
    "unpack:vehicles": "fvtt package unpack vehicles --in packs --out packs/_source/vehicles --type System --id xdz -v --clean",
    "pack:assets": "fvtt package pack assets --in packs/_source/assets --out packs --type System --id xdz -v",
    "unpack:assets": "fvtt package unpack assets --in packs --out packs/_source/assets --type System --id xdz -v --clean",
    "pack:weapons": "fvtt package pack weapons --in packs/_source/weapons --out packs --type System --id xdz -v",
    "unpack:weapons": "fvtt package unpack weapons --in packs --out packs/_source/weapons --type System --id xdz -v --clean",
    "pack:extragear": "fvtt package pack extragear --in packs/_source/extragear --out packs --type System --id xdz -v",
    "unpack:extragear": "fvtt package unpack extragear --in packs --out packs/_source/extragear --type System --id xdz -v --clean",
    "pack:tables": "fvtt package pack tables --in packs/_source/tables --out packs --type System --id xdz -v",
    "unpack:tables": "fvtt package unpack tables --in packs --out packs/_source/tables --type System --id xdz -v --clean",
    "pack:macros": "fvtt package pack macros --in packs/_source/macros --out packs --type System --id xdz -v",
    "unpack:macros": "fvtt package unpack macros --in packs --out packs/_source/macros --type System --id xdz -v --clean",
    "pack:journals": "fvtt package pack journals --in packs/_source/journals --out packs --type System --id xdz -v",
    "unpack:journals": "fvtt package unpack journals --in packs --out packs/_source/journals --type System --id xdz -v --clean"
  },
  "browserslist": [
    "last 3 versions"
  ],
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@foundryvtt/foundryvtt-cli": "^3.0.4",
    "playwright": "^1.62.1",
    "sass": "^1.77.0"
  }
}
